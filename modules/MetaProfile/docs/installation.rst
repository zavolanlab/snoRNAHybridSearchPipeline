============
Installation
============

At the command line::

    $ easy_install MetaProfile

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv MetaProfile
    $ pip install MetaProfile
